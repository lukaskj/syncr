---
- name: Working Dir tests
  groups: homelab
  tasks:
    - name: System root directory
      workingDir: /root
      logOutput: false
      steps:
        - command: ls -la
    - name: Home directory
      workingDir: "~"
      logOutput: false
      steps:
        - command: ls -la
